# Python version required to run the project.
python: "3.11.3"
# Dependencies required to build packages. This field is optional.
build_dependencies:
  - pip
  - setuptools
  - wheel==0.37.1
# Dependencies required to run the project.
dependencies:
  - torch
  - pydantic
  - lightning
  - scikit-learn
  - joblib
  - chap_core @ git+https://github.com/dhis2/chap-core.git@934bc46f32571d0e88b5831a7e5024d90a76a548
  - numpydantic
